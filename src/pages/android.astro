---
export const prerender = true;
---
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>GoPlay — Watch Live Sports | No Ads & No Buffering</title>
<meta name="description" content="GoPlay tracks matches across football, cricket, and more. Get live scores, match notifications, and multiple high-fidelity streaming links with no login required.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Teko:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700;800&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
<style is:global>
  :root {
    --pitch: #0a1310;
    --turf: #0f1f16;
    --turf-2: #132a1d;
    --card: #0f1f16;
    --line: rgba(61, 255, 138, 0.15);
    --signal: #3dff8a;
    --signal-glow: rgba(61, 255, 138, 0.3);
    --signal-dim: #008f39;
    --amber: #ffb020;
    --chalk: #eef3ee;
    --chalk-dim: #9db3a8;
    --shadow: 0 30px 60px rgba(0,0,0,0.6);
    --gradient-glow: radial-gradient(circle, rgba(61,255,138,0.12) 0%, rgba(255,176,32,0.06) 50%, transparent 100%);
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  
  html {
    scroll-behavior: smooth;
    overflow-x: hidden;
    width: 100%;
  }
  
  body {
    background: var(--pitch);
    color: var(--chalk);
    font-family: 'Inter', sans-serif;
    overflow-x: hidden;
    width: 100%;
    -webkit-font-smoothing: antialiased;
    position: relative;
  }
  
  ::selection {
    background: var(--signal);
    color: #020704;
  }

  .display {
    font-family: 'Teko', sans-serif;
    font-weight: 600;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    line-height: 0.9;
  }
  
  .mono {
    font-family: 'Space Mono', monospace;
  }

  a {
    color: inherit;
    text-decoration: none;
  }
  
  .wrap {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
  }

  /* ---------- ambient glows ---------- */
  .glow {
    position: absolute;
    width: 600px;
    height: 600px;
    background: var(--gradient-glow);
    pointer-events: none;
    z-index: 0;
    filter: blur(60px);
    opacity: 0.8;
  }
  .glow-1 { top: -100px; right: -100px; }
  .glow-2 { top: 600px; left: -200px; }
  .glow-3 { bottom: 800px; right: -200px; }
  .glow-4 { bottom: -200px; left: 10%; }

  /* ---------- pitch line texture ---------- */
  .field-lines {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 0;
    background-image:
      repeating-linear-gradient(0deg, transparent, transparent 159px, rgba(0, 255, 102, 0.015) 160px),
      radial-gradient(circle at 50% 20%, rgba(0, 255, 102, 0.04), transparent 70%);
    opacity: 0.7;
  }

  /* ---------- nav ---------- */
  header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(4, 9, 7, 0.7);
    -webkit-backdrop-filter: blur(20px);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--line);
    transition: all 0.3s ease;
  }
  
  nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px 24px;
    max-width: 1200px;
    margin: 0 auto;
  }
  
  .logo {
    display: flex;
    align-items: center;
    gap: 10px;
    font-family: 'Teko', sans-serif;
    font-size: 32px;
    font-weight: 700;
    letter-spacing: 1px;
    background: linear-gradient(135deg, var(--chalk) 60%, var(--signal) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  
  .logo .dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--signal);
    box-shadow: 0 0 15px var(--signal), 0 0 5px var(--signal);
    display: inline-block;
  }
  
  .nav-links {
    display: flex;
    gap: 36px;
    font-size: 14px;
    color: var(--chalk-dim);
    font-weight: 500;
  }
  
  .nav-links a {
    transition: color 0.2s ease, text-shadow 0.2s ease;
    position: relative;
    padding: 4px 0;
  }

  .nav-links a::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    width: 0;
    height: 1px;
    background: var(--signal);
    transition: width 0.3s ease, left 0.3s ease;
  }

  .nav-links a:hover::after {
    width: 100%;
    left: 0;
  }
  
  .nav-links a:hover {
    color: var(--signal);
  }
  
  .nav-cta {
    background: linear-gradient(135deg, var(--signal) 0%, #00d9b2 100%);
    color: #020704;
    padding: 10px 22px;
    border-radius: 8px;
    font-weight: 700;
    font-size: 14px;
    box-shadow: 0 4px 15px rgba(0, 255, 102, 0.2);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  
  .nav-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 255, 102, 0.4);
  }

  @media(max-width:768px) {
    .nav-links { display: none; }
  }

  /* ---------- hero ---------- */
  .hero {
    position: relative;
    z-index: 10;
    padding: 100px 24px 80px;
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 60px;
    align-items: center;
  }
  
  @media(max-width:992px) {
    .hero {
      grid-template-columns: 1fr;
      padding-top: 60px;
      text-align: center;
      gap: 50px;
    }
  }

  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--signal);
    font-weight: 700;
    border: 1px solid var(--line);
    background: rgba(0, 255, 102, 0.04);
    border-radius: 999px;
    padding: 6px 16px;
    margin-bottom: 24px;
    -webkit-backdrop-filter: blur(4px);
    backdrop-filter: blur(4px);
  }
  
  .eyebrow .pulse {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--signal);
    animation: pulse 1.6s infinite;
  }
  
  @keyframes pulse {
    0%, 100% {
      opacity: 1;
      box-shadow: 0 0 0 0 rgba(0, 255, 102, 0.7);
    }
    50% {
      opacity: 0.4;
      box-shadow: 0 0 0 8px rgba(0, 255, 102, 0);
    }
  }

  .hero h1 {
    font-size: clamp(32px, 8vw, 92px);
    color: var(--chalk);
    text-shadow: 0 2px 10px rgba(0,0,0,0.3);
    line-height: 1.05;
    letter-spacing: 0.2px;
  }
  
  .hero h1 .desktop-only {
    display: block;
  }
  
  .hero h1 .mobile-only {
    display: none;
  }
  
  .hero h1 .highlight {
    background: linear-gradient(135deg, var(--signal) 30%, #00ddff 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  
  @media(max-width: 992px) {
    .hero h1 .desktop-only {
      display: none;
    }
    .hero h1 .mobile-only {
      display: block;
    }
  }
  
  .hero p.sub {
    margin-top: 24px;
    font-size: 17px;
    line-height: 1.65;
    color: var(--chalk-dim);
    max-width: 520px;
  }
  
  @media(max-width:992px) {
    .hero p.sub { margin: 24px auto 0; }
  }

  @media(max-width:520px) {
    .hero p.sub {
      font-size: 15px;
      line-height: 1.6;
      margin-top: 16px;
    }
  }

  .hero-actions {
    display: flex;
    gap: 16px;
    margin-top: 36px;
    flex-wrap: wrap;
  }
  
  @media(max-width:992px) {
    .hero-actions { justify-content: center; }
  }
  
  @media(max-width:520px) {
    .hero-actions {
      flex-direction: column;
      align-items: stretch;
      gap: 12px;
      margin-top: 24px;
    }
    .hero-actions a {
      justify-content: center;
      width: 100%;
    }
  }
  
  .btn-primary {
    background: linear-gradient(135deg, var(--signal) 0%, #00e0a3 100%);
    color: #020704;
    padding: 16px 30px;
    border-radius: 12px;
    font-weight: 800;
    font-size: 15px;
    display: inline-flex;
    align-items: center;
    gap: 10px;
    box-shadow: 0 10px 25px rgba(0, 255, 102, 0.25);
    transition: transform 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275), box-shadow 0.2s ease;
  }
  
  .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 30px rgba(0, 255, 102, 0.4);
  }
  
  .btn-secondary {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--line);
    color: var(--chalk);
    padding: 16px 30px;
    border-radius: 12px;
    font-weight: 700;
    font-size: 15px;
    -webkit-backdrop-filter: blur(8px);
    backdrop-filter: blur(8px);
    transition: border-color 0.2s ease, background 0.2s ease, transform 0.2s ease;
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }
  
  .btn-secondary:hover {
    border-color: var(--signal);
    background: rgba(0, 255, 102, 0.05);
    transform: translateY(-2px);
  }

  .trust-row {
    display: flex;
    gap: 28px;
    margin-top: 44px;
    flex-wrap: wrap;
    color: var(--chalk-dim);
    font-size: 14px;
  }
  
  @media(max-width:992px) {
    .trust-row { justify-content: center; }
  }

  @media(max-width:520px) {
    .trust-row {
      display: none;
    }
  }
  
  .trust-row div {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .trust-row b {
    color: var(--signal);
    font-family: 'Space Mono', monospace;
    font-size: 16px;
  }

  /* ---------- interactive slider mockup ---------- */
  .mockup-container {
    position: relative;
    width: 100%;
    max-width: 550px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    perspective: 1000px;
    z-index: 20;
  }


  /* ---------- marquee ---------- */
  .marquee-section {
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    background: rgba(8, 18, 13, 0.4);
    -webkit-backdrop-filter: blur(5px);
    backdrop-filter: blur(5px);
    padding: 20px 0;
    overflow: hidden;
    position: relative;
    z-index: 10;
  }
  
  .marquee-track {
    display: flex;
    gap: 70px;
    white-space: nowrap;
    animation: scroll 32s linear infinite;
    width: max-content;
    will-change: transform;
  }
  
  .marquee-track span {
    font-family: 'Teko', sans-serif;
    font-size: 24px;
    font-weight: 500;
    letter-spacing: 1.5px;
    color: var(--chalk-dim);
    display: flex;
    align-items: center;
    gap: 12px;
  }
  
  .marquee-track span i {
    width: 6px;
    height: 6px;
    background: var(--signal);
    box-shadow: 0 0 10px var(--signal);
    border-radius: 50%;
  }
  
  @keyframes scroll {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }

  /* ---------- sections ---------- */
  section.block {
    padding: 120px 24px;
    position: relative;
    z-index: 10;
  }
  
  .section-head {
    max-width: 680px;
    margin-bottom: 70px;
  }
  
  .section-head .eyebrow {
    margin-bottom: 18px;
  }
  
  .section-head h2 {
    font-size: clamp(38px, 5vw, 56px);
    color: var(--chalk);
  }
  
  .section-head p {
    margin-top: 16px;
    color: var(--chalk-dim);
    font-size: 16px;
    line-height: 1.65;
  }

  /* ---------- features ---------- */
  .feature-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    background: transparent;
    border: none;
    border-radius: 0;
    overflow: visible;
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
  }
  
  .feature-card {
    padding: 40px 32px;
    position: relative;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.05) 0%, rgba(255, 255, 255, 0.01) 50%, rgba(255, 255, 255, 0) 100%), rgba(10, 14, 12, 0.65);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 12px;
    -webkit-backdrop-filter: blur(20px);
    backdrop-filter: blur(20px);
    box-shadow: inset 0 1px 0 0 rgba(255, 255, 255, 0.15), 0 15px 35px 0 rgba(0, 0, 0, 0.4);
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    contain: layout style;
  }

  .feature-card:hover {
    transform: translateY(-6px);
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.08) 0%, rgba(255, 255, 255, 0.02) 50%, rgba(255, 255, 255, 0) 100%), rgba(14, 22, 17, 0.75);
    border-color: rgba(61, 255, 138, 0.45);
    box-shadow: inset 0 1px 0 0 rgba(255, 255, 255, 0.2), 0 0 30px rgba(61, 255, 138, 0.12), 0 25px 50px rgba(0,0,0,0.6);
  }

  @media(max-width:950px) {
    .feature-grid { 
      grid-template-columns: repeat(2, 1fr); 
      gap: 20px;
    }
  }
  @media(max-width:600px) {
    .feature-grid { 
      grid-template-columns: 1fr; 
      gap: 16px;
    }
  }
  
  .feature-num {
    font-family: 'Space Mono', monospace;
    color: var(--signal);
    font-size: 13px;
    margin-bottom: 24px;
    display: inline-block;
    border: 1px solid rgba(0, 255, 102, 0.2);
    padding: 2px 8px;
    border-radius: 4px;
    background: rgba(0, 255, 102, 0.02);
  }
  
  .feature-card h3 {
    font-family: 'Teko', sans-serif;
    font-size: 28px;
    font-weight: 500;
    letter-spacing: 0.5px;
    margin-bottom: 12px;
    color: var(--chalk);
    text-transform: uppercase;
  }
  
  .feature-card p {
    color: var(--chalk-dim);
    font-size: 14.5px;
    line-height: 1.6;
  }

  /* ---------- staggered 3d showcase ---------- */
  .showcase {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }
  
  @media(max-width:992px) {
    .showcase {
      grid-template-columns: 1fr;
      gap: 60px;
    }
  }

  .phone-stack-container {
    position: relative;
    height: 620px;
    width: 100%;
    max-width: 550px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .phone-stack-item {
    position: absolute;
    width: 290px;
    aspect-ratio: 9/19;
    transition: transform 0.6s cubic-bezier(0.16, 1, 0.3, 1), filter 0.6s;
    filter: drop-shadow(0 25px 40px rgba(0,0,0,0.6));
    will-change: transform;
    contain: layout style;
  }

  .phone-stack-item img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  /* State 0: Schedule is center */
  .phone-stack-container.state-0 .phone-schedule {
    transform: scale(1) translateZ(50px);
    z-index: 3;
    filter: drop-shadow(0 35px 50px rgba(0,0,0,0.7)) drop-shadow(0 0 20px rgba(0,255,102,0.05));
  }
  .phone-stack-container.state-0 .phone-home {
    transform: translateX(130px) rotate(10deg) scale(0.85);
    z-index: 2;
    filter: drop-shadow(0 15px 25px rgba(0,0,0,0.6)) brightness(0.7);
  }
  .phone-stack-container.state-0 .phone-stream {
    transform: translateX(220px) rotate(15deg) scale(0.75);
    z-index: 1;
    filter: drop-shadow(0 10px 20px rgba(0,0,0,0.6)) brightness(0.5);
  }

  /* State 1: Home is center */
  .phone-stack-container.state-1 .phone-schedule {
    transform: translateX(-130px) rotate(-10deg) scale(0.85);
    z-index: 1;
    filter: drop-shadow(0 15px 25px rgba(0,0,0,0.6)) brightness(0.6);
  }
  .phone-stack-container.state-1 .phone-home {
    transform: scale(1) translateZ(50px);
    z-index: 3;
    filter: drop-shadow(0 35px 50px rgba(0,0,0,0.7)) drop-shadow(0 0 20px rgba(0,255,102,0.05));
  }
  .phone-stack-container.state-1 .phone-stream {
    transform: translateX(130px) rotate(10deg) scale(0.85);
    z-index: 2;
    filter: drop-shadow(0 15px 25px rgba(0,0,0,0.6)) brightness(0.7);
  }

  /* State 2: Stream is center */
  .phone-stack-container.state-2 .phone-schedule {
    transform: translateX(-220px) rotate(-15deg) scale(0.75);
    z-index: 1;
    filter: drop-shadow(0 10px 20px rgba(0,0,0,0.6)) brightness(0.5);
  }
  .phone-stack-container.state-2 .phone-home {
    transform: translateX(-130px) rotate(-10deg) scale(0.85);
    z-index: 2;
    filter: drop-shadow(0 15px 25px rgba(0,0,0,0.6)) brightness(0.7);
  }
  .phone-stack-container.state-2 .phone-stream {
    transform: scale(1) translateZ(50px);
    z-index: 3;
    filter: drop-shadow(0 35px 50px rgba(0,0,0,0.7)) drop-shadow(0 0 20px rgba(0,255,102,0.05));
  }

  @media(max-width: 520px) {
    .phone-stack-container {
      height: 450px;
    }
    .phone-stack-item {
      width: 210px;
    }
    
    /* State 0: Schedule is center */
    .phone-stack-container.state-0 .phone-home {
      transform: translateX(50px) rotate(8deg) scale(0.85);
    }
    .phone-stack-container.state-0 .phone-stream {
      transform: translateX(100px) rotate(12deg) scale(0.75);
    }

    /* State 1: Home is center */
    .phone-stack-container.state-1 .phone-schedule {
      transform: translateX(-50px) rotate(-8deg) scale(0.85);
    }
    .phone-stack-container.state-1 .phone-stream {
      transform: translateX(50px) rotate(8deg) scale(0.85);
    }

    /* State 2: Stream is center */
    .phone-stack-container.state-2 .phone-schedule {
      transform: translateX(-100px) rotate(-12deg) scale(0.75);
    }
    .phone-stack-container.state-2 .phone-home {
      transform: translateX(-50px) rotate(-8deg) scale(0.85);
    }
  }
  /* Hover expand values */
  @media (hover: hover) {
    .phone-stack-container.state-0:hover .phone-schedule {
      transform: scale(1.05) translateY(-5px);
    }
    .phone-stack-container.state-0:hover .phone-home {
      transform: translateX(200px) rotate(6deg) scale(0.9);
      filter: drop-shadow(0 20px 30px rgba(0,0,0,0.65)) brightness(0.85);
    }
    .phone-stack-container.state-0:hover .phone-stream {
      transform: translateX(290px) rotate(9deg) scale(0.8);
    }

    .phone-stack-container.state-1:hover .phone-schedule {
      transform: translateX(-200px) rotate(-6deg) scale(0.9);
      filter: drop-shadow(0 20px 30px rgba(0,0,0,0.65)) brightness(0.85);
    }
    .phone-stack-container.state-1:hover .phone-home {
      transform: scale(1.05) translateY(-5px);
    }
    .phone-stack-container.state-1:hover .phone-stream {
      transform: translateX(200px) rotate(6deg) scale(0.9);
      filter: drop-shadow(0 20px 30px rgba(0,0,0,0.65)) brightness(0.9);
    }

    .phone-stack-container.state-2:hover .phone-schedule {
      transform: translateX(-290px) rotate(-9deg) scale(0.8);
    }
    .phone-stack-container.state-2:hover .phone-home {
      transform: translateX(-200px) rotate(-6deg) scale(0.9);
      filter: drop-shadow(0 20px 30px rgba(0,0,0,0.65)) brightness(0.85);
    }
    .phone-stack-container.state-2:hover .phone-stream {
      transform: scale(1.05) translateY(-5px);
    }
  }

  .showcase-copy .checklist {
    margin-top: 32px;
    display: flex;
    flex-direction: column;
    gap: 20px;
  }
  
  .check-item {
    display: flex;
    gap: 16px;
    align-items: flex-start;
    padding: 14px 18px;
    border-radius: 12px;
    border: 1px solid transparent;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
  }

  .check-item:hover {
    background: rgba(0, 255, 102, 0.02);
    border-color: rgba(0, 255, 102, 0.1);
    transform: translateX(6px);
  }

  .check-item.active {
    background: rgba(0, 255, 102, 0.04);
    border-color: rgba(0, 255, 102, 0.2);
    box-shadow: 0 8px 30px rgba(0, 255, 102, 0.02);
  }

  .check-item.active .check-mark {
    background: var(--signal);
    color: #020704;
    box-shadow: 0 0 12px rgba(0, 255, 102, 0.4);
    border-color: var(--signal);
  }
  
  .check-mark {
    width: 24px;
    height: 24px;
    flex: none;
    border-radius: 8px;
    background: rgba(0, 255, 102, 0.08);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--signal);
    font-size: 12px;
    font-weight: 800;
    border: 1px solid rgba(0, 255, 102, 0.25);
  }
  
  .check-item p {
    color: var(--chalk-dim);
    font-size: 15px;
    line-height: 1.6;
  }
  
  .check-item b {
    color: var(--chalk);
    display: block;
    font-size: 16px;
    margin-bottom: 2px;
  }

  /* ---------- stats strip ---------- */
  .stats-strip {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
    z-index: 10;
    position: relative;
  }
  
  .stat-cell {
    padding: 44px 24px;
    text-align: center;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.05) 0%, rgba(255, 255, 255, 0.01) 50%, rgba(255, 255, 255, 0) 100%), rgba(10, 14, 12, 0.65);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 12px;
    -webkit-backdrop-filter: blur(20px);
    backdrop-filter: blur(20px);
    box-shadow: inset 0 1px 0 0 rgba(255, 255, 255, 0.15), 0 15px 35px 0 rgba(0, 0, 0, 0.4);
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    contain: layout style;
  }

  .stat-cell:hover {
    transform: translateY(-6px);
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.08) 0%, rgba(255, 255, 255, 0.02) 50%, rgba(255, 255, 255, 0) 100%), rgba(14, 22, 17, 0.75);
    border-color: rgba(61, 255, 138, 0.45);
    box-shadow: inset 0 1px 0 0 rgba(255, 255, 255, 0.2), 0 0 30px rgba(61, 255, 138, 0.12), 0 25px 50px rgba(0,0,0,0.6);
  }

  @media(max-width:950px) {
    .stats-strip { 
      grid-template-columns: repeat(2, 1fr); 
      gap: 20px;
    }
  }
  @media(max-width:480px) {
    .stats-strip { 
      grid-template-columns: 1fr; 
      gap: 16px;
    }
  }
  
  .stat-num {
    font-family: 'Teko', sans-serif;
    font-size: 64px;
    color: var(--signal);
    letter-spacing: 1px;
    text-shadow: 0 0 15px rgba(0, 255, 102, 0.2);
  }
  
  .stat-label {
    font-size: 12px;
    color: var(--chalk-dim);
    letter-spacing: 1.5px;
    text-transform: uppercase;
    margin-top: 6px;
    font-weight: 600;
  }

  /* ---------- cta download ---------- */
  .cta-block {
    text-align: center;
    padding: 80px 32px;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.04) 0%, rgba(255, 255, 255, 0.01) 50%, rgba(255, 255, 255, 0) 100%), rgba(10, 14, 12, 0.65);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 16px;
    box-shadow: inset 0 1px 0 0 rgba(255, 255, 255, 0.15), 0 30px 60px rgba(0, 0, 0, 0.5);
    -webkit-backdrop-filter: blur(20px);
    backdrop-filter: blur(20px);
    position: relative;
    z-index: 10;
    overflow: hidden;
  }

  .cta-block::after {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    right: -50%;
    bottom: -50%;
    background: radial-gradient(circle at 50% 50%, rgba(61, 255, 138, 0.06), transparent 50%);
    pointer-events: none;
    z-index: -1;
  }
  
  .cta-block h2 {
    font-size: clamp(40px, 6vw, 68px);
    color: var(--chalk);
    max-width: 800px;
    margin: 0 auto 20px;
  }
  
  .cta-block p {
    color: var(--chalk-dim);
    font-size: 17px;
    max-width: 560px;
    margin: 0 auto 40px;
    line-height: 1.6;
  }
  
  .store-badges {
    display: flex;
    gap: 20px;
    justify-content: center;
    flex-wrap: wrap;
  }
  
  .store-badge {
    display: flex;
    align-items: center;
    gap: 12px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    padding: 14px 26px;
    border-radius: 10px;
    -webkit-backdrop-filter: blur(10px);
    backdrop-filter: blur(10px);
    box-shadow: inset 0 1px 0 0 rgba(255, 255, 255, 0.1), 0 4px 10px rgba(0, 0, 0, 0.2);
    transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
  }
  
  .store-badge:hover {
    border-color: rgba(61, 255, 138, 0.5);
    background: rgba(61, 255, 138, 0.08);
    transform: translateY(-2px);
    box-shadow: inset 0 1px 0 0 rgba(255, 255, 255, 0.15), 0 10px 20px rgba(0, 255, 102, 0.05), 0 6px 15px rgba(0, 0, 0, 0.3);
  }

  .store-badge svg {
    color: var(--signal);
    flex-shrink: 0;
  }
  
  .store-badge .store-text {
    text-align: left;
  }

  .store-badge .store-sub {
    font-size: 10px;
    color: var(--chalk-dim);
    display: block;
    letter-spacing: 0.5px;
  }
  
  .store-badge .store-main {
    font-size: 17px;
    font-weight: 700;
    color: var(--chalk);
    font-family: 'Inter', sans-serif;
  }

  /* ---------- footer ---------- */
  footer {
    border-top: 1px solid var(--line);
    padding: 60px 24px 40px;
    position: relative;
    z-index: 10;
    background: rgba(4, 9, 7, 0.8);
    content-visibility: auto;
    contain-intrinsic-size: auto 420px;
  }
  
  .footer-grid {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    max-width: 1200px;
    margin: 0 auto;
    gap: 50px;
  }

  .footer-note {
    color: var(--chalk-dim);
    font-size: 13.5px;
    max-width: 520px;
    line-height: 1.65;
    margin-top: 16px;
  }
  
  .footer-links {
    display: flex;
    gap: 40px;
    font-size: 14px;
    color: var(--chalk-dim);
    justify-content: flex-end;
  }
  
  .footer-links a {
    transition: color 0.2s ease;
  }

  .footer-links a:hover {
    color: var(--signal);
  }
  
  .footer-bottom {
    max-width: 1200px;
    margin: 40px auto 0;
    padding-top: 24px;
    border-top: 1px solid rgba(255,255,255,0.05);
    font-size: 12.5px;
    color: var(--chalk-dim);
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 15px;
  }

  @media(max-width: 768px) {
    .footer-grid {
      grid-template-columns: 1fr;
      gap: 32px;
      text-align: center;
    }
    
    .footer-grid .logo {
      justify-content: center;
    }
    
    .footer-note {
      margin: 16px auto 0;
    }
    
    .footer-links {
      justify-content: center;
      gap: 24px;
      flex-wrap: wrap;
    }
    
    .footer-bottom {
      flex-direction: column;
      align-items: center;
      text-align: center;
      gap: 12px;
      margin-top: 30px;
    }
  }

  /* ---------- floating community action button ---------- */
  .floating-tg {
    position: fixed;
    bottom: 24px;
    right: 24px;
    z-index: 100;
    display: flex;
    align-items: center;
    gap: 10px;
    background: #0088cc;
    color: white;
    padding: 14px 22px;
    border-radius: 50px;
    font-weight: 700;
    font-size: 14px;
    box-shadow: 0 10px 25px rgba(0, 136, 204, 0.4);
    transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275), box-shadow 0.3s ease;
    border: 1px solid rgba(255, 255, 255, 0.15);
  }

  .floating-tg:hover {
    transform: translateY(-5px) scale(1.03);
    box-shadow: 0 15px 30px rgba(0, 136, 204, 0.6);
  }

  .floating-tg svg {
    flex-shrink: 0;
  }

  @media (max-width: 600px) {
    .floating-tg span { display: none; }
    .floating-tg { padding: 14px; border-radius: 50%; }
  }

  /* reveal-on-scroll */
  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1), transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
    will-change: opacity, transform;
  }
  
  .reveal.in {
    opacity: 1;
    transform: translateY(0);
  }

  /* Disable transition on transform once revealed so parallax scrolling is instant */
  .phone-stack-container.reveal.in {
    transition: opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1);
  }

  @media (prefers-reduced-motion: reduce) {
    * {
      animation: none !important;
      transition: none !important;
    }
    .marquee-track { animation: none; }
  }
</style>
</head>
<body>

<div class="field-lines"></div>

<div class="glow-container" style="position: absolute; inset: 0; overflow: hidden; pointer-events: none; z-index: 0;">
  <div class="glow glow-1"></div>
  <div class="glow glow-2"></div>
  <div class="glow glow-3"></div>
  <div class="glow glow-4"></div>
</div>

<header>
  <nav>
    <a href="/" class="logo"><span class="dot"></span>GOPLAY</a>
    <div class="nav-links">
      <a href="#features">Features</a>
      <a href="#app">The App</a>
      <a href="#stats">Coverage</a>
      <a href="#download">Download</a>
    </div>
    <a href="#download" class="nav-cta">Get the App</a>
  </nav>
</header>

<section class="hero">
  <div>
    <div class="eyebrow"><span class="pulse"></span> World Cup 2026 · Live Streaming</div>
    <h1 class="display">
      <span class="desktop-only">Watch FIFA<br>World Cup <span class="highlight">Live.</span></span>
      <span class="mobile-only"><span style="white-space: nowrap;">Watch FIFA World Cup</span><br><span class="highlight">Live.</span></span>
    </h1>
    <p class="sub">GoPlay tracks and streams matches across football, cricket, and more. Instantly access real-time scores, countdown schedules, and live video streams with multiple high-quality source links to bypass buffering — completely free.</p>
    <div class="hero-actions">
      <a href="#download" class="btn-primary">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
          <path d="M19.35 10.04C18.67 6.59 15.64 4 12 4 9.11 4 6.6 5.64 5.35 8.04 2.34 8.36 0 10.91 0 14c0 3.31 2.69 6 6 6h13c2.76 0 5-2.24 5-5 0-2.64-2.05-4.78-4.65-4.96zM17 13l-5 5-5-5h3V9h4v4h3z"/>
        </svg>
        Download APK
      </a>
      <a href="#app" class="btn-secondary">Explore the app</a>
    </div>
    <div class="trust-row">
      <div><b>40+</b> leagues</div>
      <div><b>100%</b> Free to watch</div>
      <div><b>No</b> registration required</div>
    </div>
  </div>

  <div class="reveal">
    <div class="mockup-container">
      <div class="phone-stack-container state-1" id="hero-stack" style="margin-bottom: 20px;">
        <div class="phone-stack-item phone-schedule">
          <img src="https://pub-0440e8e4ec654c10b2471b3b64aa4265.r2.dev/App%20Screen/Goplay%20Screen%204.webp" alt="Schedule Screen" decoding="async" fetchpriority="high" />
        </div>
        <div class="phone-stack-item phone-home">
          <img src="https://pub-0440e8e4ec654c10b2471b3b64aa4265.r2.dev/App%20Screen/Goplay%20Screen1.webp" alt="Home Dashboard Screen" decoding="async" fetchpriority="high" />
        </div>
        <div class="phone-stack-item phone-stream">
          <img id="hero-stream-img" src="https://pub-0440e8e4ec654c10b2471b3b64aa4265.r2.dev/App%20Screen/Goplay%20Screen%203.webp" alt="Live Stream Player Screen" decoding="async" fetchpriority="high" />
        </div>
      </div>
    </div>
  </div>
</section>

<div class="marquee-section">
  <div class="marquee-track" id="marquee"></div>
</div>

<section class="block" id="features">
  <div class="wrap">
    <div class="section-head reveal">
      <div class="eyebrow">Key features</div>
      <h2 class="display">Engineered for fans.<br>Optimized for speed.</h2>
      <p>Enjoy live sports streaming without dealing with invasive ads, forced accounts, or broken links. The design is simple, dark, and focused entirely on the action.</p>
    </div>

    <div class="feature-grid reveal">
      <div class="feature-card">
        <div class="feature-num mono">01</div>
        <h3>Live stream player</h3>
        <p>Built-in high-quality media player with adaptive streaming capabilities, matching your internet speed to prevent buffering.</p>
      </div>
      <div class="feature-card">
        <div class="feature-num mono">02</div>
        <h3>Multiple backup links</h3>
        <p>Switch between various broadcast hosts inside the player. If one channel slows down, tap another source to keep watching instantly.</p>
      </div>
      <div class="feature-card">
        <div class="feature-num mono">03</div>
        <h3>Home dashboard</h3>
        <p>See currently live matches at a glance with one-click watch buttons, plus a dedicated section for trending television channels.</p>
      </div>
      <div class="feature-card">
        <div class="feature-num mono">04</div>
        <h3>Countdowns &amp; Schedule</h3>
        <p>View upcoming matches with precise relative time countdowns. Know exactly how many hours remain until kickoff.</p>
      </div>
      <div class="feature-card">
        <div class="feature-num mono">05</div>
        <h3>No registration</h3>
        <p>Skip the sign-up process. Simply download, launch the application, and start streaming your favorite league within seconds.</p>
      </div>
      <div class="feature-card">
        <div class="feature-num mono">06</div>
        <h3>Stunning dark mode</h3>
        <p>An eye-friendly, distraction-free aesthetic with high-contrast active states, perfect for late-night viewing sessions.</p>
      </div>
    </div>
  </div>
</section>

<section class="block" id="app" style="padding-top:0;">
  <div class="wrap showcase">
    <div class="phone-stack-container reveal state-1" id="showcase-stack">
      <div class="phone-stack-item phone-schedule">
        <img src="https://pub-0440e8e4ec654c10b2471b3b64aa4265.r2.dev/App%20Screen/Goplay%20Screen%204.webp" alt="Schedule Screen" loading="lazy" decoding="async" />
      </div>
      <div class="phone-stack-item phone-home">
        <img src="https://pub-0440e8e4ec654c10b2471b3b64aa4265.r2.dev/App%20Screen/Goplay%20Screen1.webp" alt="Home Dashboard Screen" loading="lazy" decoding="async" />
      </div>
      <div class="phone-stack-item phone-stream">
        <img src="https://pub-0440e8e4ec654c10b2471b3b64aa4265.r2.dev/App%20Screen/Goplay%20Screen%203.webp" alt="Live Stream Player Screen" loading="lazy" decoding="async" />
      </div>
    </div>

    <div class="showcase-copy reveal">
      <div class="eyebrow">Explore the interface</div>
      <h2 class="display" style="font-size:clamp(36px,5.5vw,52px);">One application.<br>Total sports access.</h2>
      <p style="color:var(--chalk-dim);margin-top:16px;font-size:16px;line-height:1.65;max-width:480px;">
        Designed to be clean and efficient. Navigate between the home matches list, live streams, and countdown schedules using the bottom navigation bar.
      </p>
      <div class="checklist">
        <div class="check-item active">
          <div class="check-mark">✓</div>
          <div>
            <b>Interactive Home Dashboard</b>
            <p>Highlights matches starting "Live Now" with prominent CTA links, paired with a carousel of trending channels.</p>
          </div>
        </div>
        <div class="check-item">
          <div class="check-mark">✓</div>
          <div>
            <b>Backup Feed Swapping</b>
            <p>Change streaming server sources inside the channel list without exiting the media player window.</p>
          </div>
        </div>
        <div class="check-item">
          <div class="check-mark">✓</div>
          <div>
            <b>Grouped Fixture Schedules</b>
            <p>Browse tomorrow's match grids and set countdown alarms so you are always ready for the match kickoff.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="wrap" style="margin-bottom: 120px; margin-top: 40px;">
  <div class="stats-strip" id="stats">
    <div class="stat-cell reveal"><div class="stat-num display">40+</div><div class="stat-label">Leagues &amp; Tournaments</div></div>
    <div class="stat-cell reveal"><div class="stat-num display">0</div><div class="stat-label">Paid Subscriptions</div></div>
    <div class="stat-cell reveal"><div class="stat-num display">&lt; 1.5s</div><div class="stat-label">Startup Load Time</div></div>
    <div class="stat-cell reveal"><div class="stat-num display">24/7</div><div class="stat-label">Active Support</div></div>
  </div>
</div>

<div class="wrap" style="margin-bottom: 120px;">
  <section class="cta-block reveal" id="download">
    <div class="eyebrow">Free · Safe · No Signup Required</div>
    <h2 class="display">Ready to watch?<br>Get GoPlay for Android now.</h2>
    <p>Stream matches in crystal-clear definition with no monthly subscriptions. Click below to download the latest secure APK release directly.</p>
    <div class="store-badges">
      <a href="#" class="store-badge">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polygon points="5 3 19 12 5 21 5 3"></polygon>
        </svg>
        <div class="store-text">
          <span class="store-sub">INSTALL FROM</span>
          <span class="store-main">Google Play</span>
        </div>
      </a>
      <a href="https://pub-0440e8e4ec654c10b2471b3b64aa4265.r2.dev/app-release.apk" class="store-badge" download>
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
          <polyline points="7 10 12 15 17 10"></polyline>
          <line x1="12" y1="15" x2="12" y2="3"></line>
        </svg>
        <div class="store-text">
          <span class="store-sub">DOWNLOAD THE</span>
          <span class="store-main">Direct APK</span>
        </div>
      </a>
    </div>
  </section>
</div>

<footer>
  <div class="wrap">
    <div class="footer-grid">
      <div>
        <a href="/" class="logo" style="margin-bottom:16px;"><span class="dot"></span>GOPLAY</a>
        <p class="footer-note">GoPlay tracks fixture updates and facilitates high-quality links to community feeds and free-to-air broadcast networks. Content is aggregated from public index services. All brand logotypes belong to respective copyright holders.</p>
      </div>
      <div class="footer-links">
        <a href="#features">Features</a>
        <a href="#app">The App</a>
        <a href="#stats">Coverage</a>
        <a href="#download">Download</a>
      </div>
    </div>
    <div class="footer-bottom">
      <span>&copy; 2026 GoPlay. Team crests and competition names belong to their respective owners.</span>
      <span>Built for fans who just want to know the score.</span>
    </div>
  </div>
</footer>

<!-- Floating Telegram Community Button -->
<a href="https://t.me/goplay_app" target="_blank" class="floating-tg" aria-label="Join Telegram Community">
  <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
    <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm4.64 6.8c-.15 1.58-.8 5.42-1.13 7.19-.14.75-.42 1-.68 1.03-.58.05-1.02-.38-1.58-.75-.88-.58-1.38-.94-2.23-1.5-.99-.65-.35-1.01.22-1.59.15-.15 2.71-2.48 2.76-2.69.01-.03.01-.14-.07-.2-.08-.06-.19-.04-.27-.02-.12.02-1.96 1.24-5.54 3.65-.52.36-.97.54-1.34.53-.41-.01-1.21-.23-1.8-.42-.72-.24-1.3-.37-1.25-.78.03-.21.32-.43.88-.66 3.44-1.5 5.74-2.49 6.88-2.97 3.28-1.37 3.96-1.61 4.41-1.61.1 0 .32.02.46.14.12.1.15.24.17.34.02.1.02.21-.01.32z"/>
  </svg>
  <span>Join Telegram</span>
</a>

<script is:inline>
  // marquee content
  const leagues = [
    "FIFA World Cup 2026", "Premier League", "La Liga", "Serie A", 
    "Bundesliga", "Champions League", "Copa América", "IPL Cricket", 
    "T20 World Cup", "US Open", "Wimbledon", "Formula 1"
  ];
  const track = document.getElementById('marquee');
  const build = () => leagues.map(l => `<span><i></i>${l}</span>`).join('');
  track.innerHTML = build() + build();

  // reveal on scroll
  const io = new IntersectionObserver((entries)=>{
    entries.forEach(e=>{ 
      if(e.isIntersecting){ 
        e.target.classList.add('in'); 
        io.unobserve(e.target); 
      } 
    });
  }, {threshold:0.12});
  document.querySelectorAll('.reveal').forEach(el=>io.observe(el));

  // Interactive phone slider & showcase sync logic
  const heroStackContainer = document.getElementById('hero-stack');
  const heroStreamImg = document.getElementById('hero-stream-img');
  const showcaseContainer = document.getElementById('showcase-stack');
  const checklistItems = document.querySelectorAll('.check-item');
  const STREAM_IMG_SRC = "https://pub-0440e8e4ec654c10b2471b3b64aa4265.r2.dev/App%20Screen/Goplay%20Screen%203.webp";
  // index -> which stack state ("state-0/1/2") should be centered; same mapping for hero and showcase.
  const STACK_STATES = ['state-1', 'state-2', 'state-0'];
  const STATE_CLASSES = ['state-0', 'state-1', 'state-2'];
  let currentIndex = 0;
  let autoSlideInterval = null;

  // Swap the centered state class without touching any other classes
  // already on the element (e.g. preserves the scroll-triggered 'reveal'/'in'
  // classes on the showcase stack instead of forcing it into view early).
  const applyStackState = (el, index) => {
    if (!el) return;
    el.classList.remove(...STATE_CLASSES);
    el.classList.add(STACK_STATES[index]);
  };

  const setActiveScreen = (index) => {
    currentIndex = index;

    if (heroStreamImg) heroStreamImg.src = STREAM_IMG_SRC;

    applyStackState(heroStackContainer, index);
    applyStackState(showcaseContainer, index);

    checklistItems.forEach((item, idx) => item.classList.toggle('active', idx === index));
  };

  const startAutoSlide = () => {
    stopAutoSlide();
    autoSlideInterval = setInterval(() => {
      setActiveScreen((currentIndex + 1) % 3);
    }, 4500);
  };

  const stopAutoSlide = () => {
    if (autoSlideInterval) clearInterval(autoSlideInterval);
    autoSlideInterval = null;
  };

  checklistItems.forEach((item, idx) => {
    item.addEventListener('click', () => {
      setActiveScreen(idx);
      startAutoSlide();
    });
  });

  // Pause the interval while the tab isn't visible so we're not doing
  // pointless work (and layout/paint) in the background.
  document.addEventListener('visibilitychange', () => {
    if (document.hidden) {
      stopAutoSlide();
    } else {
      startAutoSlide();
    }
  });

  // Start sliding automatically
  startAutoSlide();
</script>

</body>
</html>
